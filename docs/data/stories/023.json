{
  "id": "023",
  "featureId": "f-review-ux",
  "title": "Suppress 'Tools, systems, and domains' from review UI and queue it for handoff removal",
  "owner": "ui-dev",
  "status": "In Review",
  "qa": "none",
  "qaNote": "—",
  "adrRefs": ["ADR-P6-010"],
  "epicSource": "f-review-ux · Phase 4 review UX improvements approved 2026-07-19",
  "userStory": "As a user reviewing my bootstrapped profile, I want the 'Tools, systems, and domains' section invisible on the review page and automatically included as a deletion instruction in the corrections handoff, so that this unhelpful section is removed from my profile without any manual action.",
  "acceptanceCriteria": [
    {
      "id": "AC1",
      "text": "`PhaseReviewCorrect` never renders a section card for any section whose `selectRenderer` result is `'suppressed'`. For 'Tools, systems, and domains', `HEADING_OVERRIDE_MAP` (story 019) already maps this heading to `'suppressed'`; the render loop must guard against it (e.g. `if (renderer === 'suppressed') continue`). `grep -n '\"Tools, systems\\|Tools, systems' work-hq/src/routes/onboarding.tsx` returns at least one match confirming the heading string is referenced in the suppression path. After implementation, loading the review page with a profile containing the 'Tools, systems, and domains' section produces no DOM element whose visible text includes 'Tools, systems, and domains'."
    },
    {
      "id": "AC2",
      "text": "`buildProfileCorrectionsMarkdown` unconditionally emits a section-level removal directive for 'Tools, systems, and domains' in every corrections handoff block — even when the user made no other edits. The directive follows the exact section-level removal convention defined in file-contracts §4.3 at the time of implementation: the implementer must read §4.3 before writing this line and emit the convention verbatim. Do not invent a format. `grep -n 'Tools, systems' work-hq/src/routes/onboarding.tsx` returns at least one match inside `buildProfileCorrectionsMarkdown`."
    },
    {
      "id": "AC3",
      "text": "The `Ask` block in `buildProfileCorrectionsMarkdown` is updated to append, unconditionally and after the existing ask text, the exact sentence: `Remove the ## Tools, systems, and domains section from context/profile.md entirely.` This sentence must appear verbatim — the casing, punctuation, and `##` prefix are exact. `grep -n 'Tools, systems, and domains section' work-hq/src/routes/onboarding.tsx` returns at least one match inside the Ask string literal or the `buildProfileCorrectionsMarkdown` function."
    },
    {
      "id": "AC4",
      "text": "The suppression is driven by heading string alone, not by section body content. `selectRenderer('Tools, systems, and domains', '')` returns `'suppressed'`; `selectRenderer('Tools, systems, and domains', '- some bullet')` also returns `'suppressed'`. Confirmed by `grep -n 'HEADING_OVERRIDE_MAP\\|suppressed' work-hq/src/routes/onboarding.tsx` returning the heading key in the override map declaration — not in a body-parsing or body-length conditional."
    },
    {
      "id": "AC5",
      "text": "`skills/os-helper/SKILL.md` gains a note in or immediately after the Signal C action block documenting the section-level removal directive: when the corrections handoff contains a section-level removal directive (following the file-contracts §4.3 convention, as defined by the architect before implementation), the assistant must delete the entire named section — heading line and all body content — from `context/profile.md`. The assistant does not edit or replace the section; it deletes it. The note is concise (under 8 lines). `grep -n 'section.*remov\\|entire.*section\\|heading.*delet' skills/os-helper/SKILL.md` returns at least one match."
    }
  ],
  "technicalNotes": "Depends on story 019: `selectRenderer` and `HEADING_OVERRIDE_MAP` must already map 'Tools, systems, and domains' → `'suppressed'`. The render-loop guard (AC1) requires no new code beyond ensuring `PhaseReviewCorrect` skips the renderer slot when `selectRenderer` returns `'suppressed'` — this guard is also the correct defence against future suppressed sections. The handoff change (AC2/AC3) is a targeted modification to `buildProfileCorrectionsMarkdown` (line ~657 per story 013 impl notes): (a) always append the section-level removal directive for 'Tools, systems, and domains' in the section block — read file-contracts §4.3 for the exact format before writing; (b) append the Ask sentence unconditionally after the existing ask lines. IMPORTANT: file-contracts §4.3 does not yet define the section-level removal convention as of story 023 authoring (2026-07-19) — the architect will update §4.3 before implementation. Do not begin AC2 until §4.3 carries the convention. The SKILL.md edit (AC5) is a targeted addition to `skills/os-helper/SKILL.md` after the Signal C block — model the style on story 018's edits (concise, self-contained, under 8 lines). Files touched: `work-hq/src/routes/onboarding.tsx`, `skills/os-helper/SKILL.md`.",
  "dependencies": ["019"],
  "outOfScope": "KV-grid renderer (story 020). Item-list renderer (story 021). Taxonomy picker (story 022). Suppressing any section other than 'Tools, systems, and domains'. Changing the profile-corrections handoff header line (frozen, file-contracts §4.3). Changing AGENTS.md. Implementing the section-level removal convention itself in file-contracts §4.3 — that is the architect's responsibility.",
  "verificationPlan": [
    {
      "command": "grep -n '\"Tools, systems\\|Tools, systems' work-hq/src/routes/onboarding.tsx",
      "checks": "Returns at least two matches — one in the HEADING_OVERRIDE_MAP / render dispatch (AC1/AC4) and one inside buildProfileCorrectionsMarkdown (AC2/AC3)."
    },
    {
      "command": "grep -n 'Tools, systems, and domains section' work-hq/src/routes/onboarding.tsx",
      "checks": "Returns at least one match — confirming the exact Ask sentence is present inside buildProfileCorrectionsMarkdown."
    },
    {
      "command": "grep -n 'section.*remov\\|entire.*section\\|heading.*delet' skills/os-helper/SKILL.md",
      "checks": "Returns at least one match — confirming the SKILL.md note about section-level deletion is present after the Signal C block."
    }
  ],
  "revisionLog": [],
  "impl": {
    "branch": "f-review-ux",
    "commit": "bbf55505a3e31b239d752a7a3795f6a290105b25",
    "filesTouched": [
      "work-hq/src/routes/onboarding.tsx",
      "skills/os-helper/SKILL.md",
      "docs/data/stories/023.json"
    ],
    "notes": "AC1/AC4 (review-UI suppression) were already satisfied by story 019: HEADING_OVERRIDE_MAP maps 'Tools, systems, and domains' -> 'suppressed' (onboarding.tsx ~1713); suppressed sections are excluded from review/confirmed totals (~3289-3300) and filtered from the render list (~3319), so no card and no visible text render for that heading. AC2/AC3: buildProfileCorrectionsMarkdown now unconditionally emits the block-level directive '**Removed section:** Tools, systems, and domains' as the trailing section block (per file-contracts §4.3, not a bullet) and appends the exact Ask sentence 'Remove the ## Tools, systems, and domains section from context/profile.md entirely.' after the existing Ask text. The dead 'No corrections — all sections accepted as is' branch (story 011) was removed per §4.3's no-empty-handoff clause — the handoff always carries at least the removal directive. AC5: skills/os-helper/SKILL.md Signal C block gains a section-level removal note distinguishing '**Removed section:** <heading>' (delete the entire ## section) from '- **Removed:** <fact>' (delete one bullet); SKILL.md version bumped 2.2 -> 2.3, header date refreshed. No new design tokens or components; this story is handoff-string + docs only.",
    "verificationRun": [
      "npx tsc --noEmit -> PASS (no output, exit 0)",
      "npm run --prefix work-hq build -> PASS (built in ~0.6s, exit 0)",
      "grep -n '\"Tools, systems\\|Tools, systems' work-hq/src/routes/onboarding.tsx -> PASS (matches in HEADING_OVERRIDE_MAP ~1713 and buildProfileCorrectionsMarkdown ~1004/1009)",
      "grep -n 'Tools, systems, and domains section' work-hq/src/routes/onboarding.tsx -> PASS (exact Ask sentence at ~1009)",
      "grep -n 'section.*remov\\|entire.*section\\|heading.*delet' skills/os-helper/SKILL.md -> PASS (Signal C section-level note at line 68)",
      "harness (replica of buildProfileCorrectionsMarkdown): zero-edits -> directive + Ask sentence present, dead literal absent, header regex intact; with-edits -> directive coexists with '- New scope' and '- **Removed:** Stale duty', unchanged fact omitted -> 10/10 PASS",
      "bundle grep: emitted 'No corrections' runtime literal absent from client bundle; only remaining server-bundle hit is the JSDoc comment text (not an emitted value)"
    ]
  }
}
